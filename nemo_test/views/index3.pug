extends layout

block content
  link(rel="stylesheet", href="/css/nemo3.css")
  script(src="")
  h1 네모로직 구현해보기
  form(action="/", method="POST")
    each row_num in [1, 2, 3, 4, 5]
      div.row
        each block_num in [1, 2, 3, 4, 5]
          input(type="checkbox", value="1", name=`p_block${(row_num - 1) * 5 + block_num}`)
    button(type="submit") 저장
